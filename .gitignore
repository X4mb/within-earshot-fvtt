node_modules/
*.log
.DS_Store
.env
.env.*

# Build output — produced by CI / release, never committed.
dist/

# Packaged module zips (do not commit)
*.zip

# Local third-party voice-changer app used for experiments (binaries, not module source)
voicechanger/
